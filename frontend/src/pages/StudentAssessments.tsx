import { useCallback, useEffect, useState } from 'react';
import { Alert, Box, Button, Card, CircularProgress, Divider, FormControl, FormControlLabel, LinearProgress, Radio, RadioGroup, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Typography } from '@mui/material';
import { ArrowBack, ArrowForward } from '@mui/icons-material';
import { PageHeader } from '../components/PageHeader';
import { StatusChip } from '../components/StatusChip';
import { api } from '../api/client';
import type { ApiAssessment, AssessmentAttempt } from '../api/types';
import { DataTablePagination, DataTableToolbar, SortableTableCell, useDataTable } from '../components/DataTable';
import { useStudentScope } from '../components/StudentScopeContext';

export function StudentAssessments({ onRecovery, targetCompetency, onTargetHandled }: { onRecovery: () => void; targetCompetency?: number | null; onTargetHandled?: () => void }) {
  const [assessments, setAssessments] = useState<ApiAssessment[] | null>(null);
  const [active, setActive] = useState<ApiAssessment | null>(null);
  const [attempts, setAttempts] = useState<AssessmentAttempt[]>([]);
  const [answers, setAnswers] = useState<Record<number, string>>({});
  const [questionIndex, setQuestionIndex] = useState(0);
  const [result, setResult] = useState<AssessmentAttempt | null>(null);
  const [error, setError] = useState('');
  const [busy, setBusy] = useState(false);
  const scope = useStudentScope();

  const load = useCallback(() => { if (!scope?.selectedSubjectId) return Promise.resolve(); const query = `?subject=${scope.selectedSubjectId}`; return Promise.all([api<{ results?: ApiAssessment[] } | ApiAssessment[]>(`/assessments/${query}`), api<{ results?: AssessmentAttempt[] } | AssessmentAttempt[]>(`/assessments/my-attempts/${query}`)]).then(([assessmentResult, attemptResult]) => { setAssessments(Array.isArray(assessmentResult) ? assessmentResult : assessmentResult.results ?? []); setAttempts(Array.isArray(attemptResult) ? attemptResult : attemptResult.results ?? []); }).catch(reason => setError(reason.message)); }, [scope?.selectedSubjectId]);
  useEffect(() => { setAssessments(null); setActive(null); setResult(null); void load(); }, [load]);

  const [activeCompetency, setActiveCompetency] = useState<number | null>(null);
  const start = useCallback(async (assessment: ApiAssessment, competencyId?: number | null) => {
    let resolvedCompetency = competencyId ?? null;
    if (!resolvedCompetency && assessment.kind === 'post') {
      const assessmentResults = attempts.filter(attempt => attempt.assessment === assessment.id).flatMap(attempt => attempt.competency_results);
      const nextRetry = assessment.competency_ids.map(id => assessmentResults.find(item => item.competency === id)).find(item => item && item.status !== 'mastered');
      resolvedCompetency = nextRetry?.competency ?? null;
    }
    setBusy(true); setError('');
    try { const response = await api<{ assessment: ApiAssessment }>(`/assessments/${assessment.id}/start/${resolvedCompetency ? `?competency=${resolvedCompetency}` : ''}`); setActive(response.assessment); setActiveCompetency(resolvedCompetency); setAnswers({}); setQuestionIndex(0); setResult(null); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to start assessment.'); } finally { setBusy(false); }
  }, [attempts]);
  useEffect(() => { if (!targetCompetency || !assessments || active || busy) return; const matching = assessments.find(item => item.kind === 'post' && item.available && item.competency_ids.includes(targetCompetency)); if (matching) { onTargetHandled?.(); void start(matching, targetCompetency); } }, [targetCompetency, assessments, active, busy, start, onTargetHandled]);
  const submit = async () => {
    if (!active?.questions) return;
    setBusy(true); setError('');
    try { const submitted = await api<AssessmentAttempt>(`/assessments/${active.id}/submit/`, { method: 'POST', body: JSON.stringify({ competency: activeCompetency, answers: active.questions.map(question => ({ question_id: question.id, answer: answers[question.id] })) }) }); setResult(submitted); setActive(null); setActiveCompetency(null); await load(); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to submit assessment.'); } finally { setBusy(false); }
  };
  const assessmentRows = (assessments ?? []).map(assessment => ({ assessment, previous: attempts.find(attempt => attempt.assessment === assessment.id), results: assessment.competency_ids.map(competencyId => attempts.flatMap(attempt => attempt.competency_results).find(item => item.competency === competencyId)) }));
  const table = useDataTable(assessmentRows, { searchText: row => `${row.assessment.title} ${row.assessment.kind} ${row.assessment.available ? 'available' : 'locked'}`, sortValues: { assessment: row => row.assessment.title, type: row => row.assessment.kind, questions: row => row.assessment.question_count, score: row => row.previous ? Number(row.previous.score) : null, status: row => row.previous ? 'completed' : row.assessment.available ? 'available' : 'locked' }, initialSort: 'assessment' });

  if (scope && !scope.loading && !scope.selectedSubjectId) return <><PageHeader title="Assessments" /><Alert severity="info">No subject with assigned assessments is available yet.</Alert></>;
  if (!assessments && !error) return <Box sx={{ minHeight: 360, display: 'grid', placeItems: 'center' }}><CircularProgress size={28} /></Box>;
  if (active?.questions) {
    const question = active.questions[questionIndex];
    const answeredCount = Object.keys(answers).length;
    return <><Button startIcon={<ArrowBack />} onClick={() => setActive(null)} sx={{ mb: 2, px: 0 }}>Exit assessment</Button><PageHeader title={active.title} description={`${answeredCount} of ${active.questions.length} answered`} /><Box sx={{ maxWidth: 840, mx: 'auto' }}><LinearProgress variant="determinate" value={(questionIndex + 1) / active.questions.length * 100} sx={{ height: 7, borderRadius: 1, mb: 3 }} /><Card sx={{ p: { xs: 2.5, sm: 4 } }}><Typography variant="overline" color="text.secondary" fontWeight={700}>Question {questionIndex + 1} of {active.questions.length}</Typography><Typography variant="h2" sx={{ mt: 1 }}>{question.prompt}</Typography><FormControl sx={{ mt: 2, width: '100%' }}><RadioGroup value={answers[question.id] ?? ''} onChange={event => setAnswers(current => ({ ...current, [question.id]: event.target.value }))}>{question.options.map(option => <FormControlLabel key={option} value={option} control={<Radio />} label={option} sx={{ border: '1px solid', borderColor: answers[question.id] === option ? 'primary.main' : 'divider', borderRadius: 1, m: 0, mb: 1, px: 1.5, py: .5, bgcolor: answers[question.id] === option ? '#f0f6fa' : '#fff' }} />)}</RadioGroup></FormControl><Divider sx={{ my: 3 }} /><Box sx={{ display: 'flex', justifyContent: 'space-between' }}><Button disabled={questionIndex === 0} onClick={() => setQuestionIndex(index => index - 1)}>Previous</Button>{questionIndex < active.questions.length - 1 ? <Button variant="contained" endIcon={<ArrowForward />} disabled={!answers[question.id]} onClick={() => setQuestionIndex(index => index + 1)}>Next question</Button> : <Button variant="contained" disabled={answeredCount !== active.questions.length || busy} onClick={submit}>Submit assessment</Button>}</Box></Card></Box></>;
  }
  return <><PageHeader title="Assessments" description="Complete formal assessments assigned to your class." />{error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}{result && <Alert severity="success" sx={{ mb: 2 }}>Assessment submitted. Your recorded score is {Math.round(Number(result.score))}%.</Alert>}<Alert severity="info" sx={{ mb: 2 }}>Formal assessments cover the target competencies and are separate from the short practice questions inside each recovery lesson.</Alert><Card><DataTableToolbar query={table.query} onQuery={table.setQuery} placeholder="Search assessments" count={table.filteredCount} /><TableContainer><Table sx={{ minWidth: 780 }}><TableHead><TableRow><SortableTableCell column="assessment" label="Assessment" orderBy={table.orderBy} direction={table.direction} onSort={table.toggleSort} /><SortableTableCell column="type" label="Type" orderBy={table.orderBy} direction={table.direction} onSort={table.toggleSort} /><SortableTableCell column="questions" label="Questions" orderBy={table.orderBy} direction={table.direction} onSort={table.toggleSort} /><SortableTableCell column="score" label="Latest score" orderBy={table.orderBy} direction={table.direction} onSort={table.toggleSort} /><SortableTableCell column="status" label="Status" orderBy={table.orderBy} direction={table.direction} onSort={table.toggleSort} /><TableCell align="right">Action</TableCell></TableRow></TableHead><TableBody>{table.pageRows.map(({ assessment, previous, results }) => { const allMastered = assessment.kind === 'post' && results.length > 0 && results.every(item => item?.status === 'mastered'); const needsRetry = assessment.kind === 'post' && Boolean(previous) && !allMastered; const completed = assessment.kind === 'post' ? allMastered : Boolean(previous); return <TableRow key={assessment.id}><TableCell><Typography variant="body2" fontWeight={650}>{assessment.title}</Typography>{needsRetry && <Typography variant="caption" color="warning.dark" sx={{ display: 'block', mt: .5 }}>One or more competencies still require a mastery retry.</Typography>}{!assessment.available && assessment.availability_reason && <Typography variant="caption" color="text.secondary" sx={{ display: 'block', mt: .5, maxWidth: 320 }}>{assessment.availability_reason}</Typography>}</TableCell><TableCell>{assessment.kind === 'pre' ? 'Diagnostic' : assessment.kind === 'remedial' ? 'Remedial exam' : 'Mastery assessment'}</TableCell><TableCell>{assessment.question_count}</TableCell><TableCell>{previous ? <Typography variant="body2" fontWeight={700}>{Math.round(Number(previous.score))}%</Typography> : '—'}</TableCell><TableCell><StatusChip label={completed ? 'Completed' : needsRetry ? 'Review needed' : assessment.available ? 'Available' : 'Locked'} /></TableCell><TableCell align="right">{completed ? <Typography variant="body2" color="text.secondary">No action needed</Typography> : assessment.available ? <Button size="small" variant="contained" disabled={busy} onClick={() => start(assessment)}>{needsRetry ? 'Try again' : 'Start assessment'}</Button> : assessment.kind === 'post' ? <Button size="small" variant="text" onClick={onRecovery}>View recovery plan</Button> : <Typography variant="body2" color="text.secondary">Not available</Typography>}</TableCell></TableRow>; })}</TableBody></Table></TableContainer>{!table.filteredCount && <Typography variant="body2" color="text.secondary" sx={{ p: 4, textAlign: 'center' }}>No assessments match this search.</Typography>}<DataTablePagination count={table.filteredCount} page={table.page} rowsPerPage={table.rowsPerPage} onPage={table.setPage} onRowsPerPage={table.setRowsPerPage} /></Card></>;
}
